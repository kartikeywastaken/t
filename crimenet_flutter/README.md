# CrimeNet Flutter client

Start the API in `../crimenet_backend`, then run:

```bash
flutter pub get
flutter run -d chrome
```

The app connects to `http://localhost:8000`. Use Settings to point it at a
different CrimeNet API. Default local credentials are `analyst` /
`Crimenet@123`.
